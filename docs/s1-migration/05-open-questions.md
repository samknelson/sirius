# Open Questions

Everything unresolvable from structure plus S2 knowledge. Grouped: blocking → semantic → masked-data → operational. Q#s/N#s are referenced from [02-mapping.md](02-mapping.md) and [03-transformations.md](03-transformations.md).

> **Triage pass (2026-08-04):** most remaining items decompose into (a) answered from the S2 schema / production profile artifacts — closed below; (b) answerable with aggregate-only drush queries — see **[07-prod-query-pack.md](07-prod-query-pack.md)**, section letter noted per item; (c) genuinely needing a human decision — only **N17** (needs the staged diff to exist first), **Q38** (file-storage access), possibly **N11's S2-home** and **N12's conflict rule** depending on what the queries return.
>
> **⚠️ RULINGS 2026-08-05 (fund):** **OPEN-3 CLOSED** — negative hours import as-is (BPA-era corrections, no charges generated, kept for pension vesting). **N17 CLOSED** — benefit records **import directly, do NOT rebuild** (no policy history in S1 → recompute not viable); migrate ALL raw records — NO cutoff (amended 2026-08-05: import everything); S1 remains system of record through transition. **Carelon/VSP consolidation CLOSED** — no transform at migration, carry over as-is; splitting deferred to a later S2 phase (N19 becomes later-phase input). **N11 AMENDED → N24, N24 CLOSED same day** — keep-list = exactly ONE tag, **"Comms: Received Enrollment Packet"**; the rest stay extract-and-stage. **N25 CLOSED (same day):** `employer_contacts` widened to MULTI-LINK — one row per (contact, employer, type); storage + T24 loader shipped. **N26 CLOSED (same day):** the 115 missing-start relationship rows default-date (start 2000-01-01, end 2000-01-02 unless a real end exists); the 2 future-start rows were fixed directly in S1. Validation strategy: month-parity run (06 §9 item 10). Details in 06 v5 §4.2/§4.13/§4.15/§8.
>
> **⚠️ SUPERSEDED IN BULK BY 06 v5 (2026-08-04, full production profiling pass).** [06-strategy-revision.md](06-strategy-revision.md) v5 closes most items below with live data: **N11** (tags are computed state → extract-and-stage, §4.2), **N12** (co-assignments always cross industries; delta drops, §4.8), **N16** (`changed` is a real timestamp, §4.14), **N5** (carrier consolidation — needs fund-authored alias table N19, §4.15), **N15** (empirical freeze list, §4.17), **N18** (10 legacy payperiod nids identified, §4.12), **Q10** (add language column to contacts), **Q19** (all ledger rows Cleared, §4.18), **Q29** (cold archive + SMS lifecycle types), **Q33** (secrets inventory + cutover rotation, §4.16), **Q34** (one `bookmarks` flag), **Q36** (one duplicate SSN pair; 11,839 nulls → review queue), **Q39** (no opt-out state in S1 — provider-level, blocks first send not migration), **OPEN-4** (900-series hour types never occur). **The authoritative open list is now 06 v5 §8** (N3, N4, N6, N7, N17, N19–N23, OPEN-1/2/3/5, with owners). Per-item notes below are kept for history; where they conflict with v5, **v5 wins**.

> **Revised per [06-strategy-revision.md](06-strategy-revision.md) v4.** Closed: **Q0** (real census, 06 §4.1), **Q2/Q8** (four worker identifiers — Sirius ID / Union ID / External ID / AAT, 06 §4.9), **Q14** (relationship `count` is a Sequence, 06 §4.10), **Q21** (hours located at `$.totals.hours.total` in payperiod JSON, 06 §4.12), **Q25** (grievance empty — descoped), **Q32** (`member` vestigial), **Q37** (industry encoded in member-status term names, 06 §4.8), **timezone conventions** (06 §5 — 16 LA wall time, 6 UTC), **N1/N2** (smf_worker_month JSON is autotag bookkeeping; the bundle is NOT a coverage record — but it is IN SCOPE for its tags, 06 §4.2), **N9-old** (payperiod JSON structure), **N13** (employment status is derived by design, 06 §4.8a). Structurally closed: **Q13**, **Q16**, **Q22**.
>
> **Closed as OUT OF SCOPE** (dispatch/skills, 06 §4.7): **Q3, Q4, Q5, Q6, Q7, Q9, Q22, Q23, Q24.**
>
> N-series numbering follows 06 §8 (v4).

**Standing rule applied throughout:** every remaining mapping marked *(inferred)* needs confirmation against the live system or production aggregates. Value-level checks run inside the HIPAA boundary and emit **aggregates only, never rows**.

## Blocking (must resolve before the affected ETL builds)

- **N11 — `smf_worker_month` tag vocabulary. VOCABULARY ANSWERED (drush pack §A, run 2026-08-04).** Single vocabulary `sirius_contact_tags` (59 distinct terms on this bundle, 13.55M uses; delta distribution confirms max 17 tags/row, ~2.49M rows tagged ≈ the full 2.53M spine). All 60 observed terms fall into five families and ALL are **derived autotag output**, not human-entered data: `Hours:*` (buildup/break/nonzero states — S2 benefits scan computes these live), `Election Type:*` (tier snapshot — S2 stores on elections), `Benefit:*` (benefits held that month — same facts as `sirius_trust_worker_benefit`/T17), `Plan:*` (policy in force — S2 policy resolution), `Status: Disability|FMLA` (7.6K uses — sourced from worker status, not tag-native). **Recommended S2-home: NONE — stage-only archive** (same disposition as T17): no new S2 table; the tags are monthly cached snapshots of facts S2 stores natively or recomputes; historical report needs query `s1_staging`. ~~Pending final sign-off (see T29).~~ **AMENDED + RULED 2026-08-05 (N24 CLOSED):** keep-list = exactly ONE tag, **"Comms: Received Enrollment Packet"** (S2 home decided at T29 build; offline-comm record per tagged worker-month is the leading candidate); the rest stay stage-only. Duplicate term names exist (two "Benefit: Express Scripts" tids 1641/1686) — irrelevant under stage-only.
- **N12 — `member_status` target model. S2 TARGET CONFIRMED to exist and fit:** `options_worker_ms` (`sirius_id`, `industry_id`, `sequence`) + `worker_msh` (current = latest row **per industry**). Mapping: term → `options_worker_ms` by `sirius_id`, one `worker_msh` row per assignment. The only thing that can break it is a worker holding two concurrent terms in the SAME industry — **drush pack §B** measures exactly that. Sam is needed only if same-industry pairs exist.
- ~~**N17 — Benefit-history decision: import vs regenerate-and-reconcile** (06 §4.13).~~ **CLOSED (2026-08-05): IMPORT DIRECTLY** — no policy history in S1, recompute not viable; ALL raw records migrate — NO cutoff (amended 2026-08-05: import everything); S1 stays system of record through transition. Validation = month-parity run (06 §9 item 10), not the regenerate-vs-actual diff.
- **N3 — `sirius_employer_payperiod` (18,395). SUBSTANTIALLY ANSWERED from the profile:** its complete field set is employer ref + `date_start`/`date_end` + `datetime`/`datetime_completed` + `active` + `domain` — **no amounts, no JSON**. It is a pure period header (employer reporting/remittance period tracking), which maps to `wizard_employer_monthly` (employer_id, year, month) or drops (S2 recreates wizard state per import). Confirm the UI role from S1 knowledge + **drush pack §N**.
- **Q38 — File blobs.** `file_managed` URIs are `private://` (+ `s3fs_file`, 1,030 rows). The DB has no bytes; T10 needs access to the S1 private-files directory / S3 bucket. *(genuinely external — infra access)*
- **N15 — Cutover freeze writer list.** Known: benefit scanner (old Q18) + `sirius_smf.wym.inc`. **Drush pack §D** enumerates ALL bundles with creates/changes in the last 14 days — the complete freeze list, empirically.

## Newly visible bundles needing targets

- **N4 — `sirius_employee` (539). SUBSTANTIALLY ANSWERED from the profile:** complete field set is worker ref + shop ref + `field_sirius_id` + domain — a worker↔employer employment link carrying an external employee code. S2 has employment rows per (worker, employer) with a home flag (`worker_employment` denorm family). Confirm which UI feature uses it + **drush pack §N**.
- **N5 — `sirius_trust_provider` (12). MAPPING CLOSED:** fields are title + `active` + address(+geo) + `json` + domain → S2 `trust_providers` (`name`, `data` jsonb: `{siriusId, active, address}`). Carrier names: **drush pack §K** (or eyeball the 12 in the UI).
- **N6 — Balance parity must reconcile `sirius_ledger_ar` (~547k rows) and `sirius_payment` (3,458) together** — both carry real activity (06 §4.6); neither is complete alone. Sign conventions + status inputs for the test design: **drush pack §G**.
- **N7 — configured-but-empty bundles. CLOSED (profile artifacts):** all 20 bundles have **zero `field_data_*` AND zero `field_revision_*` rows** — nothing exists to recover, purged-or-never-used is moot. Out of ETL scope, no revision recovery possible. (The 06 §4.1 "16 vs 20" count discrepancy remains a doc nit only.)
- **N8 — title-only micro-bundles. CLOSED (profile artifacts):** `sirius_twilio_conversation` (8), `grievance_company` (3), `grievance_chapter` (2) carry **no field rows at all** — title-only nodes. Disposition: DROP with logged nids/titles. Eyeball titles via **drush pack §K** (twilio titles may hold phone numbers — shape-check only).
- **N10-old — Contact-style fields directly on `sirius_worker`** (name 117,679 / email 17,009 / phone 36,319 / address 51,962 rows *on the worker bundle*, alongside the referenced `sirius_contact` node). Which record wins when both exist? **Drush pack §L** measures overlap + agreement (email as proxy); if worker copies are denorm mirrors, contact wins and this closes.

## Identifiers & semantics still open

- **N14 — AAT and Titan ID. CLOSED (mmcdermott4 2026-08-04: ignore).** Migrate both as **opaque identifier strings** — AAT → `worker_ids` row (type "AAT", no validator), Titan ID label on work-status `field_sirius_id2` carries over as-is. No owner documentation needed; drush pack §C is now optional (only if we later want a format validator).
- **N18 — The 10 legacy-format payperiod rows** (06 §4.12) — **drush pack §F** identifies the nids + periods; then disposition individually (T20 skips with logged reason regardless).
- **N16 — `node.changed` uniform ~Oct 2023.** Previously attributed to an anonymization artifact; earliest `smf_worker_month` node is 2023-10-30, so Oct 2023 may be a real system event (data load / migration into S1), in which case `changed` is meaningful. **Drush pack §E.**
- **Q15 — `field_sirius_id` on `sirius_trust_benefit` / `sirius_ledger_account` / `sirius_phonenumber` / `sirius_employee`.** A per-entity external code; meanings differ per bundle (06 §4.9: "Call Number" on dispatch jobs — out of scope; "ListID" on member-status terms). Decide per bundle once meaning is confirmed (on `sirius_phonenumber` likely the number itself — Q12).
- **Q19 — `sirius_ledger_ar.ledger_key`** (sample showed mixed `test`/numeric ids) and whether non-`Cleared` `ledger_status` values exist in production; S2's `ledger` has no status column, so any pending/void statuses need a policy. **Drush pack §G.**
- **Q1 — Masked-in-sample columns catalog.** All columns previously flagged `MASKED` (bodies, messages/notes/json, `field_sirius_source`, `gender_nota_val`, `ledger_memo/_json`, address subcolumns, `users.data`, `role` names, `variable` names). Confirm content class with production aggregates before mapping.
- **Q10 — Preferred language (`field_sirius_lang`)** multi-valued (en, es). S2 has no language column — add one (contacts) or stash in `data`? Affects comms. Distribution: **drush pack §M.**
- **Q11 — `field_sirius_contact_tags` on contacts** — max delta **26** (544,243 field rows). Distinct question from N11 (worker-month tags): S2's `options_comm_tags` are per-message. Add a contact-tags feature, or drop? Term frequencies: **drush pack §M.**
- **Q12 — `sirius_phonenumber` number location** — `field_sirius_id` (every node) or `node.title`; and what `field_sirius_json` holds. Shape checks: **drush pack §M.** Plus the misspelled `sirius_phonenubmer` bundle (7 title-only nodes): migrate or skip with logged reason.
- **Q26 — `field_grievance_external_id`** on shops — employer number from an external system? Decide S2 home (`employers.data` vs `companies.sirius_id`).
- **Q27 — Grievance holidays** (`grievance_holiday`, 2 nodes) — only relevant if S2's greenfield grievance build wants them.
- **Q28 — Grievance vocab fine-mapping** — only the vocabs serving *populated* entities still matter (document types for files, alert/log types if reused).
- **Q30 — `sirius_news`** (5 nodes) — announcements feature wanted in S2, or drop?
- **Q31 — `sirius_json_definition`** (144 nodes) — S1's public JSON/webservice definitions vs S2 `ws_bundles`; are external consumers still calling S1 endpoints that must be re-pointed? Titles: **drush pack §K**; live-consumer question is knowledge/ops.
- **Q34 — `flag` types. NEARLY CLOSED (profile artifacts):** production has exactly **1 flag type and 245 flaggings** (`flag_types` is empty). One name lookup (**drush pack §J**) settles bookmark-vs-marker.
- **Q35 — Grievance comments. CLOSED (profile artifacts):** the `comment` table has **3 rows** in all of production. Disposition: drop with logged reason (grievance is descoped; 3 comments do not justify an archive path).
- **Q39 — Opt-outs.** No email/SMS opt-out fields observed in structure. Where does S1 store unsubscribe state (critical before S2 sends anything to migrated contacts)? **Drush pack §I** sweeps field names + `variable` names + `sms/voice_possible` distributions.

## Data-quality / operational

- **Q20 — Balance parity.** Payment sign conventions and `date_cleared` availability. Acceptance test: per-participant, per-account balance in S2 == S1 (`sirius_ledger_ar` + payments) to the cent — scope with N6. Inputs: **drush pack §G.**
- **Q29 — Log retention.** Which `sirius_log` categories (1.78M nodes) must stay queryable in S2 (vs cold archive)? Category distribution: **drush pack §M.** Also whether bulk sends link to their per-recipient log rows.
- **Q33 — `variable` table** (3,439 rows). Contains what look like Twilio SIDs and phone numbers; never bulk-migrate (credentials belong in S2 secrets, not `variables`). Name inventory (names only, never values): **drush pack §I.**
- **Q36 — SSN collisions. CONFIRMED RISK (06 §4.4):** `field_sirius_ssn_value` is `varchar(255) DEFAULT NULL` — not fixed-width, not unique, nullable; format variance unconstrained. S2's unique constraint will reject duplicates. Format/duplicate aggregates: **drush pack §H**; then a business rule (skip, suffix, or worker-merge review queue) sized by the duplicate count.
- **`ledger_memo` charset (06 §4.5/§4.11):** 97 of 261,640 rows contain non-ASCII bytes in a latin1 column; UTF-8→latin1 transcoding at write time means possible pre-existing corruption. ETL must fail loudly on decode problems, log affected `ledger_id`s, never coerce silently; characters outside latin1 (CJK, emoji) were lost at write time and are unrecoverable.
- **Q16-old/Q17-old re-check:** fields absent from the sample may simply have been missing from the export — re-check against the real table list where a mapping still assumes absence.

## Unresolved (2026-08 real-data rehearsal)

- **`employer_unresolved` residue — 1,462 benefit-history spans.** The T16 typed-elections fix let `employerFromElection` rescue 33,881 of the original 35,103; the remaining 1,462 shopless spans were allowed for the rehearsal only. Production disposition needs a fund ruling: drop vs attach to a designated employer.
- **Conrad duplicate rate.** Shop 8865846 (CONRAD LOS ANGELES, H0305) carries BOTH 5.75 and 6.50 for 2023-12-01 under the same instance (the S1 UI report shows the same duplicate pair). The fund must delete the wrong row in S1 (correct value never stated); then re-stage `grievance_shop` and rerun RUNBOOK §4 row 5c. Never allow `rate_conflict` — that would drop Conrad's entire rate history and Conrad is an active hourly employer.
- **58 active-flagged dangling-relation spans.** Within the 71,964 `relation_unmapped` rejects, 58 spans still carry active=Yes (54 open-ended) though their relationship node was deleted. The 58-nid list was delivered to the fund for S1 cleanup (end-date or confirm intentional removal); until S1 is fixed and re-staged they stay rejects.
